{
  "_comment": [
    "DEPLOYMENT CONFIG. One place for the facts a build needs but a designer",
    "cannot invent. Everything domain-dependent — canonical, og:url, hreflang,",
    "sitemap.xml, the robots Sitemap line and the analytics domain — is generated",
    "from `url`, and is SKIPPED while it is empty rather than emitted with a",
    "placeholder. A wrong canonical is worse than a missing one: it tells search",
    "engines the real page lives somewhere else.",
    "",
    "`hsts` was FALSE until the four live checks passed on 4 September 2026:",
    "brotli, the CSP, Referrer-Policy and the custom 404 all confirmed served by",
    "the live host. It is now true — the APEX ONLY. Set it to the string \"all\"",
    "to add includeSubDomains, but only once every subdomain of this host is",
    "known to serve HTTPS: one that cannot is unreachable for a year.",
    "",
    "The original warning, which still governs any NEW domain this moves to:",
    "`hsts` STAYS FALSE until check 4 in docs/44-launch.md §2 shows the live site",
    "serving TLS and its headers. The header is cached by browsers for a year:",
    "switch it on over a host that cannot serve https and visitors are locked out",
    "of the site, not merely inconvenienced. Once verified, set it to true and",
    "run `node build.js` — one field, not a hand-edited server file.",
    "",
    "MOVING DOMAIN (to pixora.net or anything else) is this one field plus",
    "`node build.js`. Nothing else in the repository names a host. The rest of",
    "the move — 301s from the old host, renaming the site in Plausible, Search",
    "Console's change of address — is in docs/44-launch.md §3.",
    "",
    "NOTE the review surface: GitHub Pages serves the modular source and ignores",
    ".htaccess, so no CSP, no rewrites and no custom 404 there. It is for reading",
    "the pages, not for judging the deployment."
  ],
  "url": "https://zaokalyamamah.online",
  "hsts": true,
  "_build": "fonts: 'inline' ships each face in the document as a data URI (AD-06, one request paints the page). 'linked' copies the .woff2 files to assets/fonts/ and points at them: the homepage drops from 134.8KB to 73.0KB gzipped, fonts cache once for the whole site, and unicode-range starts working so an English visitor never downloads the 30.9KB Arabic face. The cost is a brief flash of the fallback face, which inlining does not have. See docs/43 §15.",
  "build": {
    "fonts": "inline"
  },

  "pages": [
    {
      "file": "index.html",
      "priority": "1.0",
      "changefreq": "monthly",
      "index": true
    },
    {
      "file": "pricing.html",
      "priority": "0.9",
      "changefreq": "monthly",
      "index": true
    },
    {
      "file": "about.html",
      "priority": "0.6",
      "changefreq": "yearly",
      "index": true
    },
    {
      "file": "story.html",
      "priority": "0.8",
      "changefreq": "yearly",
      "index": true
    },
    {
      "file": "styleguide.html",
      "index": false
    },
    {
      "file": "privacy.html",
      "priority": "0.3",
      "changefreq": "yearly",
      "index": true
    },
    {
      "file": "terms.html",
      "priority": "0.3",
      "changefreq": "yearly",
      "index": true
    },
    {
      "file": "accessibility.html",
      "priority": "0.3",
      "changefreq": "yearly",
      "index": true
    }
  ],
  "contact": {
    "_comment": "WhatsApp number in wa.me form (digits only, country code first). One place: build-pricing.js renders every package CTA from it.",
    "whatsapp": "249962672192",
    "_emailComment": [
      "The address the SITE publishes. It is declared here so that qa.js can",
      "assert no page publishes any other one — the address is written into",
      "navigation-map.js AND into the prose of Privacy, Terms and Accessibility",
      "in both languages, which is five places and no generator.",
      "",
      "It is NOT a source that generates those pages: an address inside a",
      "bilingual sentence cannot be templated without making the sentence a",
      "template. So this is the declared truth and the check is what keeps the",
      "copies honest. Change this field and qa.js names every page that still",
      "disagrees."
    ],
    "email": "muhalabsalah@gmail.com"
  },
  "analytics": {
    "_comment": [
      "ANALYTICS. Off until a provider is named here — choosing who receives a",
      "visitor's data is not a default a build script should pick. Set `provider`",
      "to \"plausible\" (or \"umami\") and fill in the fields below; build-deploy.js",
      "then adds the provider's script to the deployed pages, and src/scripts/",
      "analytics.js starts reporting the four events it already collects.",
      "",
      "Both options are cookieless and send no personal data, which is why they",
      "need no consent banner under most readings — but a privacy policy naming",
      "the provider must ship in the same release.",
      "",
      "Note the trade: this is the only third-party request the site makes. The",
      "zero-request property is spent deliberately, on the one thing the project",
      "cannot proceed without."
    ],
    "provider": "plausible",
    "domain": "zaokalyamamah.online",
    "scriptUrl": "https://plausible.io/js/script.js",
    "websiteId": ""
  }
}
